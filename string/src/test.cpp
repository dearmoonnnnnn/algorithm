#include <iostream>  // 不包含 iostream
// #include <string>    // 不包含 string

int main() {
    int[10] x = "Hello, World!";  // 编译器可能会报错
    // std::cout << str << std::endl;  // 不使用 iostream 的内容
    return 0;
}
